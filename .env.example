# PostgreSQL settings for local development
DB_ENGINE=postgresql
DB_NAME=bookshop
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=127.0.0.1
DB_PORT=5432

# Optionally use a full DATABASE_URL instead of individual settings
# DATABASE_URL=postgres://postgres:postgres@127.0.0.1:5432/bookshop
